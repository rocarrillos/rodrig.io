import { render, screen } from '@testing-library/react';
import { createContext } from 'react';
import ResponsiveAppBar from '../ResponsiveAppBar';

test('renders ResponsiveAppBar with all its links', () => {
    expect(true).toBe(true);
});
